import { PaginationDto } from 'src/shared/dto/pagination.dto';

/**
 * Data Transfer Object for filtering employees with pagination.
 */
export class EmployeeFilterDto extends PaginationDto {
  // No additional fields are required apart from pagination properties.
}
